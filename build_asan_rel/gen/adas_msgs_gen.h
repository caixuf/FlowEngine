/*
 * AUTO-GENERATED by FlowEngine msg_codegen.py — DO NOT EDIT
 * Source: msg/adas_msgs.msg
 */

#ifndef ADAS_MSGS_GEN_H
#define ADAS_MSGS_GEN_H

#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <stddef.h>
#include "serializer.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Enum: ObstacleType */
typedef enum {
    OBJ_TYPE_UNKNOWN = 0,
    OBJ_TYPE_VEHICLE = 1,
    OBJ_TYPE_PEDESTRIAN = 2,
    OBJ_TYPE_CYCLIST = 3,
} ObstacleType;

/* Enum: Gear */
typedef enum {
    GEAR_REVERSE = -1,
    GEAR_NEUTRAL = 0,
    GEAR_DRIVE = 1,
} Gear;

/* ──────────────────────────────────────────────────────── */
/* Struct: LidarFrame (size=24B, type_id=0xd712aa51) */
/* Sig: LidarFrame:float_x,float_y,float_z,float_intensity,uint32_point_count,uint32_fra... */

#define LIDARFRAME_TYPE_ID         0xd712aa51u
#define LIDARFRAME_SCHEMA_VERSION  1
#define LIDARFRAME_SCHEMA_HASH     0x5946acc7u
#define LIDARFRAME_TYPE_NAME        "LidarFrame"

typedef struct {
    float    x;  /**< float */
    float    y;  /**< float */
    float    z;  /**< float */
    float    intensity;  /**< float */
    uint32_t    point_count;  /**< uint32 */
    uint32_t    frame_id;  /**< uint32 */
} LidarFrame;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<LidarFrame>(msg) */
template<> struct msg_traits<LidarFrame> {
    static constexpr uint32_t TYPE_ID = LIDARFRAME_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = LIDARFRAME_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "LidarFrame";
};
extern "C" {
#endif

/** Serialize LidarFrame to buffer (endian-aware). */
static inline int LidarFrame_serialize(const LidarFrame* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 24;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->x, 4);
    if (buf) memcpy(buf + 4, &src->y, 4);
    if (buf) memcpy(buf + 8, &src->z, 4);
    if (buf) memcpy(buf + 12, &src->intensity, 4);
    if (buf) memcpy(buf + 16, &src->point_count, 4);
    if (buf) memcpy(buf + 20, &src->frame_id, 4);
    return 0;
}

/** Deserialize LidarFrame from buffer (endian-aware). */
static inline int LidarFrame_deserialize(LidarFrame* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 24) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->x, buf + 0, 4);
    memcpy(&dst->y, buf + 4, 4);
    memcpy(&dst->z, buf + 8, 4);
    memcpy(&dst->intensity, buf + 12, 4);
    memcpy(&dst->point_count, buf + 16, 4);
    memcpy(&dst->frame_id, buf + 20, 4);
    return 0;
}

/** In-place endian swap for LidarFrame. */
static inline void LidarFrame_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap32(p + 0);
    serializer_swap32(p + 4);
    serializer_swap32(p + 8);
    serializer_swap32(p + 12);
    serializer_swap32(p + 16);
    serializer_swap32(p + 20);
}

/** Field-level schema descriptor table for LidarFrame. */
static const FieldDesc LidarFrame_fields[] = {
    { "x", FIELD_KIND_FLOAT, (uint16_t)offsetof(LidarFrame, x), 4, 1 },
    { "y", FIELD_KIND_FLOAT, (uint16_t)offsetof(LidarFrame, y), 4, 1 },
    { "z", FIELD_KIND_FLOAT, (uint16_t)offsetof(LidarFrame, z), 4, 1 },
    { "intensity", FIELD_KIND_FLOAT, (uint16_t)offsetof(LidarFrame, intensity), 4, 1 },
    { "point_count", FIELD_KIND_UINT, (uint16_t)offsetof(LidarFrame, point_count), 4, 1 },
    { "frame_id", FIELD_KIND_UINT, (uint16_t)offsetof(LidarFrame, frame_id), 4, 1 },
};
#define LIDARFRAME_FIELD_COUNT  (sizeof(LidarFrame_fields)/sizeof(LidarFrame_fields[0]))

/** Auto-register LidarFrame in type registry. */
static inline void LidarFrame_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = LIDARFRAME_TYPE_ID,
        .schema_version = LIDARFRAME_SCHEMA_VERSION,
        .struct_size    = sizeof(LidarFrame),
        .type_name      = "LidarFrame",
        .serialize      = (SerializeFunc)LidarFrame_serialize,
        .deserialize    = (DeserializeFunc)LidarFrame_deserialize,
        .endian_swap    = (EndianSwapFunc)LidarFrame_endian_swap,
        .schema_hash    = LIDARFRAME_SCHEMA_HASH,
        .fields         = LidarFrame_fields,
        .field_count    = (uint16_t)LIDARFRAME_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ──────────────────────────────────────────────────────── */
/* Struct: GpsData (size=28B, type_id=0x0596b0b7) */
/* Sig: GpsData:float64_latitude,float64_longitude,float_speed_mps,float_heading_deg,flo... */

#define GPSDATA_TYPE_ID         0x0596b0b7u
#define GPSDATA_SCHEMA_VERSION  1
#define GPSDATA_SCHEMA_HASH     0xd9be1a46u
#define GPSDATA_TYPE_NAME        "GpsData"

typedef struct {
    double    latitude;  /**< float64 */
    double    longitude;  /**< float64 */
    float    speed_mps;  /**< float */
    float    heading_deg;  /**< float */
    float    accuracy_m;  /**< float */
} GpsData;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<GpsData>(msg) */
template<> struct msg_traits<GpsData> {
    static constexpr uint32_t TYPE_ID = GPSDATA_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = GPSDATA_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "GpsData";
};
extern "C" {
#endif

/** Serialize GpsData to buffer (endian-aware). */
static inline int GpsData_serialize(const GpsData* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 28;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->latitude, 8);
    if (buf) memcpy(buf + 8, &src->longitude, 8);
    if (buf) memcpy(buf + 16, &src->speed_mps, 4);
    if (buf) memcpy(buf + 20, &src->heading_deg, 4);
    if (buf) memcpy(buf + 24, &src->accuracy_m, 4);
    return 0;
}

/** Deserialize GpsData from buffer (endian-aware). */
static inline int GpsData_deserialize(GpsData* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 28) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->latitude, buf + 0, 8);
    memcpy(&dst->longitude, buf + 8, 8);
    memcpy(&dst->speed_mps, buf + 16, 4);
    memcpy(&dst->heading_deg, buf + 20, 4);
    memcpy(&dst->accuracy_m, buf + 24, 4);
    return 0;
}

/** In-place endian swap for GpsData. */
static inline void GpsData_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap64(p + 0);
    serializer_swap64(p + 8);
    serializer_swap32(p + 16);
    serializer_swap32(p + 20);
    serializer_swap32(p + 24);
}

/** Field-level schema descriptor table for GpsData. */
static const FieldDesc GpsData_fields[] = {
    { "latitude", FIELD_KIND_FLOAT, (uint16_t)offsetof(GpsData, latitude), 8, 1 },
    { "longitude", FIELD_KIND_FLOAT, (uint16_t)offsetof(GpsData, longitude), 8, 1 },
    { "speed_mps", FIELD_KIND_FLOAT, (uint16_t)offsetof(GpsData, speed_mps), 4, 1 },
    { "heading_deg", FIELD_KIND_FLOAT, (uint16_t)offsetof(GpsData, heading_deg), 4, 1 },
    { "accuracy_m", FIELD_KIND_FLOAT, (uint16_t)offsetof(GpsData, accuracy_m), 4, 1 },
};
#define GPSDATA_FIELD_COUNT  (sizeof(GpsData_fields)/sizeof(GpsData_fields[0]))

/** Auto-register GpsData in type registry. */
static inline void GpsData_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = GPSDATA_TYPE_ID,
        .schema_version = GPSDATA_SCHEMA_VERSION,
        .struct_size    = sizeof(GpsData),
        .type_name      = "GpsData",
        .serialize      = (SerializeFunc)GpsData_serialize,
        .deserialize    = (DeserializeFunc)GpsData_deserialize,
        .endian_swap    = (EndianSwapFunc)GpsData_endian_swap,
        .schema_hash    = GPSDATA_SCHEMA_HASH,
        .fields         = GpsData_fields,
        .field_count    = (uint16_t)GPSDATA_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ──────────────────────────────────────────────────────── */
/* Struct: Obstacle (size=33B, type_id=0x5d941266) */
/* Sig: Obstacle:uint32_id,float_x,float_y,float_vx,float_vy,float_width,float_length,Ob... */

#define OBSTACLE_TYPE_ID         0x5d941266u
#define OBSTACLE_SCHEMA_VERSION  1
#define OBSTACLE_SCHEMA_HASH     0xa5b34fdbu
#define OBSTACLE_TYPE_NAME        "Obstacle"

typedef struct {
    uint32_t    id;  /**< uint32 */
    float    x;  /**< float */
    float    y;  /**< float */
    float    vx;  /**< float */
    float    vy;  /**< float */
    float    width;  /**< float */
    float    length;  /**< float */
    int8_t    type;  /**< ObstacleType */
    float    confidence;  /**< float */
} Obstacle;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<Obstacle>(msg) */
template<> struct msg_traits<Obstacle> {
    static constexpr uint32_t TYPE_ID = OBSTACLE_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = OBSTACLE_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "Obstacle";
};
extern "C" {
#endif

/** Serialize Obstacle to buffer (endian-aware). */
static inline int Obstacle_serialize(const Obstacle* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 33;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->id, 4);
    if (buf) memcpy(buf + 4, &src->x, 4);
    if (buf) memcpy(buf + 8, &src->y, 4);
    if (buf) memcpy(buf + 12, &src->vx, 4);
    if (buf) memcpy(buf + 16, &src->vy, 4);
    if (buf) memcpy(buf + 20, &src->width, 4);
    if (buf) memcpy(buf + 24, &src->length, 4);
    if (buf) buf[28] = (int8_t)src->type;
    if (buf) memcpy(buf + 29, &src->confidence, 4);
    return 0;
}

/** Deserialize Obstacle from buffer (endian-aware). */
static inline int Obstacle_deserialize(Obstacle* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 33) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->id, buf + 0, 4);
    memcpy(&dst->x, buf + 4, 4);
    memcpy(&dst->y, buf + 8, 4);
    memcpy(&dst->vx, buf + 12, 4);
    memcpy(&dst->vy, buf + 16, 4);
    memcpy(&dst->width, buf + 20, 4);
    memcpy(&dst->length, buf + 24, 4);
    dst->type = (int8_t)((int8_t)buf[28]);
    memcpy(&dst->confidence, buf + 29, 4);
    return 0;
}

/** In-place endian swap for Obstacle. */
static inline void Obstacle_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap32(p + 0);
    serializer_swap32(p + 4);
    serializer_swap32(p + 8);
    serializer_swap32(p + 12);
    serializer_swap32(p + 16);
    serializer_swap32(p + 20);
    serializer_swap32(p + 24);
    serializer_swap32(p + 29);
}

/** Field-level schema descriptor table for Obstacle. */
static const FieldDesc Obstacle_fields[] = {
    { "id", FIELD_KIND_UINT, (uint16_t)offsetof(Obstacle, id), 4, 1 },
    { "x", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, x), 4, 1 },
    { "y", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, y), 4, 1 },
    { "vx", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, vx), 4, 1 },
    { "vy", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, vy), 4, 1 },
    { "width", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, width), 4, 1 },
    { "length", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, length), 4, 1 },
    { "type", FIELD_KIND_ENUM, (uint16_t)offsetof(Obstacle, type), 1, 1 },
    { "confidence", FIELD_KIND_FLOAT, (uint16_t)offsetof(Obstacle, confidence), 4, 1 },
};
#define OBSTACLE_FIELD_COUNT  (sizeof(Obstacle_fields)/sizeof(Obstacle_fields[0]))

/** Auto-register Obstacle in type registry. */
static inline void Obstacle_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = OBSTACLE_TYPE_ID,
        .schema_version = OBSTACLE_SCHEMA_VERSION,
        .struct_size    = sizeof(Obstacle),
        .type_name      = "Obstacle",
        .serialize      = (SerializeFunc)Obstacle_serialize,
        .deserialize    = (DeserializeFunc)Obstacle_deserialize,
        .endian_swap    = (EndianSwapFunc)Obstacle_endian_swap,
        .schema_hash    = OBSTACLE_SCHEMA_HASH,
        .fields         = Obstacle_fields,
        .field_count    = (uint16_t)OBSTACLE_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ──────────────────────────────────────────────────────── */
/* Struct: ObstacleList (size=280B, type_id=0x308f5f71) */
/* Sig: ObstacleList:uint32_frame_id,uint64_timestamp_us,uint32_count,Obstacle:uint32_id... */

#define OBSTACLELIST_TYPE_ID         0x308f5f71u
#define OBSTACLELIST_SCHEMA_VERSION  1
#define OBSTACLELIST_SCHEMA_HASH     0x7260c39du
#define OBSTACLELIST_TYPE_NAME        "ObstacleList"

typedef struct {
    uint32_t    frame_id;  /**< uint32 */
    uint64_t    timestamp_us;  /**< uint64 */
    uint32_t    count;  /**< uint32 */
    Obstacle    obstacles[8];  /**< Obstacle */
} ObstacleList;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<ObstacleList>(msg) */
template<> struct msg_traits<ObstacleList> {
    static constexpr uint32_t TYPE_ID = OBSTACLELIST_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = OBSTACLELIST_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "ObstacleList";
};
extern "C" {
#endif

/** Serialize ObstacleList to buffer (endian-aware). */
static inline int ObstacleList_serialize(const ObstacleList* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 280;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->frame_id, 4);
    if (buf) memcpy(buf + 4, &src->timestamp_us, 8);
    if (buf) memcpy(buf + 12, &src->count, 4);
    { /* obstacles[0] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[0], buf ? buf + 16 : NULL, &sz);
    }
    { /* obstacles[1] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[1], buf ? buf + 49 : NULL, &sz);
    }
    { /* obstacles[2] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[2], buf ? buf + 82 : NULL, &sz);
    }
    { /* obstacles[3] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[3], buf ? buf + 115 : NULL, &sz);
    }
    { /* obstacles[4] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[4], buf ? buf + 148 : NULL, &sz);
    }
    { /* obstacles[5] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[5], buf ? buf + 181 : NULL, &sz);
    }
    { /* obstacles[6] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[6], buf ? buf + 214 : NULL, &sz);
    }
    { /* obstacles[7] */
        size_t sz = 0;
        Obstacle_serialize(&src->obstacles[7], buf ? buf + 247 : NULL, &sz);
    }
    return 0;
}

/** Deserialize ObstacleList from buffer (endian-aware). */
static inline int ObstacleList_deserialize(ObstacleList* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 280) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->frame_id, buf + 0, 4);
    memcpy(&dst->timestamp_us, buf + 4, 8);
    memcpy(&dst->count, buf + 12, 4);
    { /* obstacles[0] */
        Obstacle_deserialize(&dst->obstacles[0], buf + 16, 33);
    }
    { /* obstacles[1] */
        Obstacle_deserialize(&dst->obstacles[1], buf + 49, 33);
    }
    { /* obstacles[2] */
        Obstacle_deserialize(&dst->obstacles[2], buf + 82, 33);
    }
    { /* obstacles[3] */
        Obstacle_deserialize(&dst->obstacles[3], buf + 115, 33);
    }
    { /* obstacles[4] */
        Obstacle_deserialize(&dst->obstacles[4], buf + 148, 33);
    }
    { /* obstacles[5] */
        Obstacle_deserialize(&dst->obstacles[5], buf + 181, 33);
    }
    { /* obstacles[6] */
        Obstacle_deserialize(&dst->obstacles[6], buf + 214, 33);
    }
    { /* obstacles[7] */
        Obstacle_deserialize(&dst->obstacles[7], buf + 247, 33);
    }
    return 0;
}

/** In-place endian swap for ObstacleList. */
static inline void ObstacleList_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap32(p + 0);
    serializer_swap64(p + 4);
    serializer_swap32(p + 12);
    Obstacle_endian_swap(p + 16);
    Obstacle_endian_swap(p + 49);
    Obstacle_endian_swap(p + 82);
    Obstacle_endian_swap(p + 115);
    Obstacle_endian_swap(p + 148);
    Obstacle_endian_swap(p + 181);
    Obstacle_endian_swap(p + 214);
    Obstacle_endian_swap(p + 247);
}

/** Field-level schema descriptor table for ObstacleList. */
static const FieldDesc ObstacleList_fields[] = {
    { "frame_id", FIELD_KIND_UINT, (uint16_t)offsetof(ObstacleList, frame_id), 4, 1 },
    { "timestamp_us", FIELD_KIND_UINT, (uint16_t)offsetof(ObstacleList, timestamp_us), 8, 1 },
    { "count", FIELD_KIND_UINT, (uint16_t)offsetof(ObstacleList, count), 4, 1 },
    { "obstacles", FIELD_KIND_NESTED, (uint16_t)offsetof(ObstacleList, obstacles), 33, 8 },
};
#define OBSTACLELIST_FIELD_COUNT  (sizeof(ObstacleList_fields)/sizeof(ObstacleList_fields[0]))

/** Auto-register ObstacleList in type registry. */
static inline void ObstacleList_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = OBSTACLELIST_TYPE_ID,
        .schema_version = OBSTACLELIST_SCHEMA_VERSION,
        .struct_size    = sizeof(ObstacleList),
        .type_name      = "ObstacleList",
        .serialize      = (SerializeFunc)ObstacleList_serialize,
        .deserialize    = (DeserializeFunc)ObstacleList_deserialize,
        .endian_swap    = (EndianSwapFunc)ObstacleList_endian_swap,
        .schema_hash    = OBSTACLELIST_SCHEMA_HASH,
        .fields         = ObstacleList_fields,
        .field_count    = (uint16_t)OBSTACLELIST_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ──────────────────────────────────────────────────────── */
/* Struct: EgoState (size=32B, type_id=0x212dcff7) */
/* Sig: EgoState:float64_latitude,float64_longitude,float_heading_deg,float_speed_mps,fl... */

#define EGOSTATE_TYPE_ID         0x212dcff7u
#define EGOSTATE_SCHEMA_VERSION  1
#define EGOSTATE_SCHEMA_HASH     0xb3550a53u
#define EGOSTATE_TYPE_NAME        "EgoState"

typedef struct {
    double    latitude;  /**< float64 */
    double    longitude;  /**< float64 */
    float    heading_deg;  /**< float */
    float    speed_mps;  /**< float */
    float    yaw_rate_dps;  /**< float */
    float    acceleration_mss;  /**< float */
} EgoState;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<EgoState>(msg) */
template<> struct msg_traits<EgoState> {
    static constexpr uint32_t TYPE_ID = EGOSTATE_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = EGOSTATE_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "EgoState";
};
extern "C" {
#endif

/** Serialize EgoState to buffer (endian-aware). */
static inline int EgoState_serialize(const EgoState* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 32;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->latitude, 8);
    if (buf) memcpy(buf + 8, &src->longitude, 8);
    if (buf) memcpy(buf + 16, &src->heading_deg, 4);
    if (buf) memcpy(buf + 20, &src->speed_mps, 4);
    if (buf) memcpy(buf + 24, &src->yaw_rate_dps, 4);
    if (buf) memcpy(buf + 28, &src->acceleration_mss, 4);
    return 0;
}

/** Deserialize EgoState from buffer (endian-aware). */
static inline int EgoState_deserialize(EgoState* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 32) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->latitude, buf + 0, 8);
    memcpy(&dst->longitude, buf + 8, 8);
    memcpy(&dst->heading_deg, buf + 16, 4);
    memcpy(&dst->speed_mps, buf + 20, 4);
    memcpy(&dst->yaw_rate_dps, buf + 24, 4);
    memcpy(&dst->acceleration_mss, buf + 28, 4);
    return 0;
}

/** In-place endian swap for EgoState. */
static inline void EgoState_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap64(p + 0);
    serializer_swap64(p + 8);
    serializer_swap32(p + 16);
    serializer_swap32(p + 20);
    serializer_swap32(p + 24);
    serializer_swap32(p + 28);
}

/** Field-level schema descriptor table for EgoState. */
static const FieldDesc EgoState_fields[] = {
    { "latitude", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, latitude), 8, 1 },
    { "longitude", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, longitude), 8, 1 },
    { "heading_deg", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, heading_deg), 4, 1 },
    { "speed_mps", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, speed_mps), 4, 1 },
    { "yaw_rate_dps", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, yaw_rate_dps), 4, 1 },
    { "acceleration_mss", FIELD_KIND_FLOAT, (uint16_t)offsetof(EgoState, acceleration_mss), 4, 1 },
};
#define EGOSTATE_FIELD_COUNT  (sizeof(EgoState_fields)/sizeof(EgoState_fields[0]))

/** Auto-register EgoState in type registry. */
static inline void EgoState_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = EGOSTATE_TYPE_ID,
        .schema_version = EGOSTATE_SCHEMA_VERSION,
        .struct_size    = sizeof(EgoState),
        .type_name      = "EgoState",
        .serialize      = (SerializeFunc)EgoState_serialize,
        .deserialize    = (DeserializeFunc)EgoState_deserialize,
        .endian_swap    = (EndianSwapFunc)EgoState_endian_swap,
        .schema_hash    = EGOSTATE_SCHEMA_HASH,
        .fields         = EgoState_fields,
        .field_count    = (uint16_t)EGOSTATE_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ──────────────────────────────────────────────────────── */
/* Struct: ControlCmd (size=18B, type_id=0x2d95c6d2) */
/* Sig: ControlCmd:uint32_seq,float_throttle,float_brake,float_steering,Gear_gear,bool_e... */

#define CONTROLCMD_TYPE_ID         0x2d95c6d2u
#define CONTROLCMD_SCHEMA_VERSION  1
#define CONTROLCMD_SCHEMA_HASH     0x8303b978u
#define CONTROLCMD_TYPE_NAME        "ControlCmd"

typedef struct {
    uint32_t    seq;  /**< uint32 */
    float    throttle;  /**< float */
    float    brake;  /**< float */
    float    steering;  /**< float */
    int8_t    gear;  /**< Gear */
    bool    emergency_stop;  /**< bool */
} ControlCmd;

#ifdef __cplusplus
}  /* close extern "C" — template specialization needs C++ linkage */
/* C++ type traits for msg_cast<ControlCmd>(msg) */
template<> struct msg_traits<ControlCmd> {
    static constexpr uint32_t TYPE_ID = CONTROLCMD_TYPE_ID;
    static constexpr uint8_t  SCHEMA_VERSION = CONTROLCMD_SCHEMA_VERSION;
    static constexpr const char* TYPE_NAME = "ControlCmd";
};
extern "C" {
#endif

/** Serialize ControlCmd to buffer (endian-aware). */
static inline int ControlCmd_serialize(const ControlCmd* src,
        uint8_t* buf, size_t* out_size) {
    if (!src) return -1;
    size_t total = 18;
    if (out_size) *out_size = total;
    if (!buf) return 0;  /* size query only */

    if (buf) memcpy(buf + 0, &src->seq, 4);
    if (buf) memcpy(buf + 4, &src->throttle, 4);
    if (buf) memcpy(buf + 8, &src->brake, 4);
    if (buf) memcpy(buf + 12, &src->steering, 4);
    if (buf) buf[16] = (int8_t)src->gear;
    if (buf) buf[17] = (uint8_t)src->emergency_stop;
    return 0;
}

/** Deserialize ControlCmd from buffer (endian-aware). */
static inline int ControlCmd_deserialize(ControlCmd* dst,
        const uint8_t* buf, size_t size) {
    if (!dst || !buf) return -1;
    if (size < 18) return -1;
    memset(dst, 0, sizeof(*dst));

    memcpy(&dst->seq, buf + 0, 4);
    memcpy(&dst->throttle, buf + 4, 4);
    memcpy(&dst->brake, buf + 8, 4);
    memcpy(&dst->steering, buf + 12, 4);
    dst->gear = (int8_t)((int8_t)buf[16]);
    dst->emergency_stop = (bool)buf[17];
    return 0;
}

/** In-place endian swap for ControlCmd. */
static inline void ControlCmd_endian_swap(void* data) {
    if (!data) return;
    uint8_t* p = (uint8_t*)data;
    size_t off = 0;

    serializer_swap32(p + 0);
    serializer_swap32(p + 4);
    serializer_swap32(p + 8);
    serializer_swap32(p + 12);
}

/** Field-level schema descriptor table for ControlCmd. */
static const FieldDesc ControlCmd_fields[] = {
    { "seq", FIELD_KIND_UINT, (uint16_t)offsetof(ControlCmd, seq), 4, 1 },
    { "throttle", FIELD_KIND_FLOAT, (uint16_t)offsetof(ControlCmd, throttle), 4, 1 },
    { "brake", FIELD_KIND_FLOAT, (uint16_t)offsetof(ControlCmd, brake), 4, 1 },
    { "steering", FIELD_KIND_FLOAT, (uint16_t)offsetof(ControlCmd, steering), 4, 1 },
    { "gear", FIELD_KIND_ENUM, (uint16_t)offsetof(ControlCmd, gear), 1, 1 },
    { "emergency_stop", FIELD_KIND_BOOL, (uint16_t)offsetof(ControlCmd, emergency_stop), 1, 1 },
};
#define CONTROLCMD_FIELD_COUNT  (sizeof(ControlCmd_fields)/sizeof(ControlCmd_fields[0]))

/** Auto-register ControlCmd in type registry. */
static inline void ControlCmd_register_type(void) {
    TypeRegistryEntry e = {
        .type_id        = CONTROLCMD_TYPE_ID,
        .schema_version = CONTROLCMD_SCHEMA_VERSION,
        .struct_size    = sizeof(ControlCmd),
        .type_name      = "ControlCmd",
        .serialize      = (SerializeFunc)ControlCmd_serialize,
        .deserialize    = (DeserializeFunc)ControlCmd_deserialize,
        .endian_swap    = (EndianSwapFunc)ControlCmd_endian_swap,
        .schema_hash    = CONTROLCMD_SCHEMA_HASH,
        .fields         = ControlCmd_fields,
        .field_count    = (uint16_t)CONTROLCMD_FIELD_COUNT,
    };
    serializer_register_type(&e);
}

/* ── Batch registration ─────────────────────────────── */

/**
 * Register all ADAS message types in the type registry.
 * Call once during initialization (idempotent).
 */
static inline void adas_msgs_register_all(void) {
    LidarFrame_register_type();
    GpsData_register_type();
    Obstacle_register_type();
    ObstacleList_register_type();
    EgoState_register_type();
    ControlCmd_register_type();
}


#ifdef __cplusplus
}
#endif

#endif /* ADAS_MSGS_GEN_H */